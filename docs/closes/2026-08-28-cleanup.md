# Close state — 2026-08-28 cleanup session (rulings 1-6)

Report: `docs/2026-08-28-cleanup.md`.

## State verification at open — matched; tree clean; writes permitted

| pointer | expected | measured | |
|---|---|---|---|
| HEAD | at or past `1605482` | `1605482638c6bfb47a12e2f2d5efb4da21f6c2c5` | ✅ |
| Suite | 1051 | **1051 passed** | ✅ |
| Tree | clean | clean | ✅ |
| Unpushed | — | 0 | ✅ |
| caliber.db md5 | — | `8752e75e05a8a7ff225258ec99d36fc3` | ✅ |
| PID | — | **own pid 14893**, PPID walk from bash child 16327 | ✅ |

No peers, no `ppid 1` orphans. WAL checkpointed `(0,0,0)` before each md5; wal/shm pair
removed and md5 re-verified after.

## State at close

| | |
|---|---|
| HEAD | the **cleanup close commit** — verify with `git log -1`. Previous was `1605482`. |
| Suite | **1089 passed** (was 1051; +38). No pre-existing test broke. **7 of 38 verified to FAIL against disarmed fixes.** |
| caliber.db md5 | **`8752e75e05a8a7ff225258ec99d36fc3` — UNCHANGED** |
| **PRODUCTION WRITES** | **ZERO.** No write path was executed. All ten tables re-counted at close and identical: evaluations 80 · lifecycle_stage 44 · lifecycle_transitions 1 · field_provenance 1437 · fundamental_series 2622 · grades 0 · overrides 0 · lifecycle_overrides 0 · synthesis_cache 16 · stage_flip_approvals 1. |
| Backup | none taken — nothing was written. `caliber.db.pre-qbts-approval-19d615fe.bak` from the prior session stands. |

## Outcomes by ruling

| # | ruling | outcome |
|---|---|---|
| 1 | feed repair | **Premise inverted.** XE closed by measurement (feed now contiguous FY2021-25 both endpoints). IONQ FY2020 **is served** (`revenue: 0`) and dropped by *our* line-156 guard. Typed reason split `revenue_zero` / `no_revenue`; **drop behaviour unchanged**. Admission NOT taken — dark run flips **INFQ** `YOUNG → MATURE`. |
| 2 | ETF guard | **ARMED** (`core/etf_guard.py`, exit 7 / `status='etf_refused'`). **0 of 28 caught, 0 UNKNOWN.** Positive control fires on LYTE and FLTW. |
| 3 | provenance relabel | **Already done.** 0 live strings (AST sweep: docstrings only). **0 rows touched, 45 left** (dated 2026-07-12 / 2026-08-07). Regression pin added. |
| 4 | dead surfaces | **Already gone** — neither file exists. Real residue fixed: `edgar_adapter` no longer tells the operator to "Run probe.py first". |
| 5 | pin retirement | `BANK-RUNG-UNCALIBRATED` retired by name, reason `tripwire:unreachable_population_gated`. Production flag untouched. **Successor re-arms and fails the suite if any bank stops being gated.** |
| 6 | doctrine amendment | The "or" struck in all three places; sanity gate **defined as** the 25% divergence check; explicit prohibition on building new machinery. |

## Files touched

- `core/lifecycle.py` — typed reason split (drop behaviour unchanged).
- `core/etf_guard.py` — **NEW**.
- `core/datatypes.py` — `is_etf` tri-state field.
- `adapters/fmp_adapter.py` — populates `is_etf` from `profile.isEtf`.
- `adapters/edgar_adapter.py` — stale operator guidance corrected.
- `evaluate.py`, `batch/runner.py` — ETF refusal armed above `select_lens` and above the writer.
- `tests/test_d3_dark_lenses.py` — retirement recorded in place, naming its successor.
- `tests/test_2026_08_28_cleanup.py` — **NEW**, 38 pins.
- `CLAUDE.md` — doctrine amendment ×3; two stale punch-list claims corrected.
- `docs/2026-08-28-cleanup.md`, `docs/closes/2026-08-28-cleanup.md`, `docs/closes/README.md`.

## Open

1. `revenue_zero` admission — evidence in report §1, Vic's call.
2. Census §8 has **7 stale items** (1, 3, 9, 20, 25, 26, 27) — reconcile at the acceptance session.
3. ETF guard cannot catch a fund whose payload omits `isEtf`. Bounded and named.
