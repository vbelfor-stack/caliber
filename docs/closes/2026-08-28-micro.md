# Close state — 2026-08-28 micro session (QBTS approval · GOOG diagnosis · close-state migration)

Order: chat-issued (Vic, 2026-08-28). Report: `docs/2026-08-28-micro.md`.

## State verification at open — ALL FOUR POINTERS MATCHED, no mismatch, writes permitted

| pointer | expected | measured | |
|---|---|---|---|
| HEAD | `ea01d12` | `ea01d122ee60eec96295fff3c051f46ea22aa312` | ✅ |
| Suite | 1051 | **1051 passed** | ✅ |
| caliber.db md5 | `19d615fe…` | `19d615fed542e1804fbb4bf0405878b1` | ✅ |
| tree / unpushed | clean / 0 | clean; `origin/master..master` = 0 | ✅ |
| PID | — | **own pid 14893**, verified by PPID walk from bash child 15046 | ✅ |

Peer-process check: one `claude` process (my own). **No peers. No `ppid 1` orphans** running
python / evaluate.py / batch.runner. WAL checkpointed `(0,0,0)` before every md5; the empty
wal/shm pair read connections create was removed and the md5 re-verified after.

## State at close

| | |
|---|---|
| HEAD | the **micro-session close commit** — verify with `git log -1`. Previous was `ea01d12`. |
| Suite | **1051 passed** — unchanged. No test added, none broken. |
| caliber.db md5 | **`8752e75e05a8a7ff225258ec99d36fc3`** (was `19d615fe…`) |
| Backup | `caliber.db.pre-qbts-approval-19d615fe.bak`, md5 verified equal to the pre-write db |
| **PRODUCTION WRITES** | **ONE: +1 row in `stage_flip_approvals`** (the QBTS approval, id 1). Expected delta stated before the write, dry-run against a copy first, reconciled exactly. |
| stage_flip_approvals | **1 row** (was 0) — QBTS `HIGROWTH → YOUNG` |
| sqlite_sequence | **6 rows** (was 5) — new AUTOINCREMENT entry `('stage_flip_approvals', 1)`, the standing dependent |
| lifecycle_stage | **44 rows, UNCHANGED** — QBTS id 28 still reads `HIGROWTH`, `run_at` 2026-08-17, `retired_reason` NULL, byte-for-byte |
| evaluations / lifecycle_transitions / field_provenance / fundamental_series / grades / overrides / lifecycle_overrides / synthesis_cache | 80 / 1 / 1437 / 2622 / 0 / 0 / 0 / 16 — **all +0**, re-counted at close |
| step-4 evaluable | **20 of 28 — unchanged.** `SELECT DISTINCT ticker` still reads 23; still do not use it. |

**Expected-delta reconciliation.** Stated before writing: `stage_flip_approvals` 0 → 1;
`sqlite_sequence` 5 → 6; every other table +0. Dry-run on `/tmp/dryrun.db` (a copy of
production) diffed against production across **every table in `sqlite_master`**: exactly two
deltas, both as stated, and QBTS's `lifecycle_stage` rows compared **equal**. Production run
then reproduced it. **Expected 2 table deltas, actual 2 — MATCH.**

## Pre-flight

The `--approve` path calls `recompute()`, which fetches **live EDGAR and live FMP**. Per the
standing live-EDGAR discipline it was pre-flighted on the **adapter's own fetch path**
immediately before the run (not curl): `fetch_edgar("QBTS")` → cik `0001907982`, sic `7374`;
`fetch_fmp("QBTS")` → Technology / Computer Hardware. Both clean.

## Files touched

- `adapters/fmp_adapter.py` — **comment only**, the GOOG mislabel correction. Zero behaviour
  change; `ast.parse` clean; suite unchanged at 1051.
- `CLAUDE.md` — 1504 → 1491 lines (net −13; the three tables came out, the standing rule
  and pointer block went in). Three state tables relocated; QBTS references updated;
  standing rule + close-protocol step added.
- `docs/closes/` — NEW. `README.md` index + four close files.
- `docs/2026-08-28-micro.md` — the session report.
