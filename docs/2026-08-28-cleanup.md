# 2026-08-28 — CLEANUP SESSION REPORT
### Six bounded rulings. **Zero production writes. caliber.db md5 UNCHANGED.**

Close state: `docs/closes/2026-08-28-cleanup.md`. Suite **1051 → 1089** (+38, 7 verified to
fail pre-fix). No pre-existing test broke.

---

## 0. STATE VERIFICATION

HEAD `1605482` ✅ (at expected) · suite **1051** ✅ · md5 `8752e75e…` · tree **clean** ·
0 unpushed ✅. Own **PID 14893**, verified by PPID walk from bash child 16327. One `claude`
process, no peers, no `ppid 1` orphans.

---

## ★★ THE HEADLINE: THREE OF THE SIX TICKETS WERE ALREADY CLOSED, AND ONE INVERTED

**Rulings 3 and 4 required no work — the conditions they described no longer existed.**
Ruling 1's premise was **false in the opposite direction**: the data was never missing.
Only rulings 2, 5 and 6 were live.

This is worth saying plainly because **"we fixed it" and "it was never broken" are different
claims**, and a census that records the first when the second is true will send a later
session hunting for a defect that is not there.

---

## 1. RULING 1 — FEED REPAIR. **THE PREMISE INVERTED. NEITHER GAP IS A FEED GAP.**

### XE — ✅ CLOSED BY MEASUREMENT. The feed repaired itself.

FMP now serves **FY2021–FY2025 contiguous on BOTH endpoints** — income and cash-flow, five
rows each, every period populated. Through our own reader: **5 admitted, 0 refused, holes =
NONE.** The doctrine order measured 3 rows (2025, 2022, 2021) on 2026-08-22; it is 5 today.
**Nothing to fill, nothing to block, no write needed.**

### IONQ — ★ NOT MISSING. FMP SERVES FY2020 AND **WE** DROPPED IT.

FMP serves IONQ FY2020: income `revenue: 0`, cash-flow `ocf: -12,007,000`. The hole is ours,
at **`core/lifecycle.py:156`**:

```python
rev = None if rev in (None, 0) else float(rev)     # a filed ZERO is treated as ABSENT
...
return None, "no_revenue"                          # ...and reported as "no revenue"
```

**IONQ's FY2020 revenue genuinely was zero** — a pre-revenue quantum company. So the stored
reason was a **false claim about the issuer for something we declined**: exactly the
`WITHHELD_NO_CAPEX` / `WITHHELD_NO_OCF` mislabel class L-4d **deleted rather than renamed**.

### ★★ AND THE DROP MUST STAY. THE GUARD IS LOAD-BEARING — MEASURED, NOT ASSUMED.

Ruling 1 permits "real data or typed block". The real data is `0`, so the tempting move is to
admit it. **Dark-run across all 28 says do not.** Exactly two names carry a zero-revenue FY:

| name | zero FYs | dark result if admitted |
|---|---|---|
| IONQ | FY2020 (interior) | `YOUNG/rule2_young` → `YOUNG/rule2_young`. **No movement.** Only `fy_count` 6 → 7. |
| XE | none | `MATURE/rule4_mature` → unchanged. |
| **INFQ** | **FY2025 + FY2024 — its two MOST RECENT** | **`YOUNG/rule2_young_insufficient_history` → `MATURE/rule4_mature`. A STAGE FLIP.** |

**INFQ is the "a zero revenue would manufacture a 100% decline" hazard the guard was written
for, firing on a live name.** Its `fy_count` goes 1 → 3 and it acquires a
`decline_streak` of 0 where it previously had none — a name with essentially no usable
history reclassified as MATURE on two placeholder-shaped zeros.

**IONQ's hole is the PRICE of the guard that protects INFQ.** Widening it is a scoring-path
ruling, not a feed repair, and it is **not taken here**.

### WHAT LANDED: the typed reason, and nothing else

`revenue_zero` (a value we declined) is now distinct from `no_revenue` (a genuine gap).
**Drop behaviour byte-identical; admitted series identical for all three names.**

```
IONQ: admitted 2019,2021-2025   refused ['2020:revenue_zero']
INFQ: admitted 2023             refused ['2025:revenue_zero','2024:revenue_zero']
XE  : admitted 2021-2025        refused none
```

**OPEN FOR VIC:** should `revenue_zero` rows ever be admitted? Evidence is above and the
answer today is clearly no. Recorded, not decided.

---

## 2. RULING 2 — ETF GUARD. **ARMED. `core/etf_guard.py`.**

Reads `profile.isEtf`, which production **already fetched and threw away**. Refuses with
`etf:not_a_company`; **exit 7** interactively, `status='etf_refused'` in batch.

### Measured across the universe, as ordered

**0 of 28 caught — as expected. 0 UNKNOWN** (all 28 report `isEtf` explicitly, so the guard
never leans on its unknown branch).

### ★ POSITIVE CONTROL — a guard that cannot fire proves nothing

```
LYTE: is_etf=True  refused=True  'Roundhill Photonics & Optics ETF'  etf:not_a_company
FLTW: is_etf=True  refused=True  'Franklin FTSE Taiwan ETF'          etf:not_a_company
```

Both are **held** and were kept out of `tickers.txt` **by hand** to route around this gap.
That workaround is now redundant — and it was the fragile part: the day someone adds a fund,
the hand stops working. The guard reads the issuer's own type, so it covers funds nobody has
thought of yet.

### ★★ `bool("false")` IS `True` — SO TRUTHINESS IS NEVER USED

"Any true value refuses" is **parsed**, tri-state, with explicit true/false sets. A truthiness
implementation would refuse **every name whose feed serves the string `"false"`** — i.e. the
whole universe — while looking like it worked. 16 parametrised cases pin it.

### ★ UNKNOWN DOES NOT REFUSE — a stated departure from fail-closed

Fail-closed would refuse every payload lacking the key: **every recorded fixture** (they
predate the field) and every live name the moment FMP drops it. Vic's ruling is "any TRUE
value refuses"; absence is not a true value. **Cost, named: this guard cannot catch a fund
whose payload omits `isEtf`.**

### Ordering is the ruling

Both paths refuse **above `select_lens`** — a fund carries a sector and industry like anything
else, so the selector would route one to a company lens and everything downstream would look
well-formed. Batch also refuses **above `run_dark_fcf_series`**, a WRITER. Both pinned by line
index. The guard owns **no taxonomy**: no name matching, no exchange rule, no ticker list —
pinned over the AST (a "Trust"/"Fund" substring rule is the `_CYCLICAL_INDUSTRY` defect that
put IONQ and INFQ on the wrong lens).

---

## 3. RULING 3 — PROVENANCE RELABEL. ✅ **ALREADY DONE. ZERO LIVE STRINGS.**

| | |
|---|---|
| Live `TickerData` Prov sources (NVDA) | **25 of 25 read `fmp`** |
| `analyze_technicals` | takes `feed_source` as a **parameter**; both call sites pass `yf.feed_source` → stamps `fmp/price_history` |
| AST sweep, all non-test non-vendor code | `yfinance` appears in **DOCSTRINGS ONLY** (6, in 4 files) |

**Rows touched: 0. Rows left: 45** (`yfinance/earnings_history` 30, `yfinance/insider_transactions` 15), from evals dated **2026-07-12 (24)** and **2026-08-07 (21)**. 1,323 rows read `fmp*` of 1,437 total. Historical rows untouched per ruling — supersede, never purge.

**★ THE DOCSTRINGS MUST NOT BE "CORRECTED".** They are the record of the yfinance teardown
and of the **ratio-vs-percent D/E unit defect** that ran eight days behind 654 green tests.
The regression pin is therefore **AST-based and exempts docstrings** — a text scan would fire
on the prose and push a later session to delete the record of a production defect. Third time
this session shape has bitten this project; it is now pinned, not remembered.

---

## 4. RULING 4 — DEAD SURFACES. ✅ **ALREADY GONE. ONE REAL RESIDUE FIXED.**

`probe.py` and `probe_fmp.py` **do not exist** — both deleted at the fixture migration
(`tools/record_edgar_fixture.py:5` and `docs/phase-archive.md:199` both say so). Nothing to
delete.

**★ THE RESIDUE WAS THE PART THAT COST SOMETHING:** `adapters/edgar_adapter.py:943` told the
operator *"Run probe.py first."* — guidance naming a file deleted a phase earlier, which costs
whoever follows it a debugging session. It now names `python -m tools.record_edgar_fixture`.

**NOT IN SCOPE, REPORTED:** `probe_mu_edgar.py` exists at repo root (Jul 10, MU CIK re-probe).
It is **not** named in ruling 4, has **no** yfinance import and **no** v3 URL. Left alone.

---

## 5. RULING 5 — `BANK-RUNG-UNCALIBRATED` RETIRED BY NAME

**Typed reason: `tripwire:unreachable_population_gated`.** It never failed and was never
wrong. It was a live-eval tripwire, and the financials gate refuses BK/C/JPM/USB before any
pillar is scored — those four are the **entire** bank-lens population. **A tripwire whose
trigger population is structurally empty is a belief with a flag attached.**

- **The production `flags.append` in `core/pillars.py` is UNTOUCHED.** Deleting it would be
  unwinding a demoted path, which needs its own ruling.
- **The unit assertion is KEPT** — it calls `score_valuation` directly, bypassing the gate,
  and pins the flag to the right rungs for the day the population returns.
- **★ THE SUCCESSOR IS AN ENFORCEMENT POINT, NOT A NOTE.**
  `test_the_BANK_RUNG_tripwire_REARMS_if_the_financials_leg_ships` recomputes membership
  **live through `fcf_model_applicability`** — never hardcoded — and **fails the suite the
  moment any bank stops being gated.** That is precisely when the cheap rungs become
  reachable and the calibration question is live again. *A retirement whose premise can
  quietly expire is one a later session weakens instead of heeding.*

---

## 6. RULING 6 — DOCTRINE AMENDMENT (census item 9), DOC-LEVEL

§1.2(a) read *"FMP fails a sanity gate **or** diverges >25%"* — naming **two** triggers where
only one existed, the first with no referent anywhere in the runtime. **The "or" is struck.
The sanity gate IS the 25% divergence check.** Amended in all three places it appeared
(doctrine §1.2, the §5 note, the session-open checklist step 4), with an explicit
**"DO NOT 'IMPLEMENT THE SANITY GATE' — there is nothing left to implement"**, so a later
session does not build a second checker against a clause that was only ever loose wording.

---

## 7. WHAT I DID NOT DO

- **Did not admit `revenue_zero` rows** — INFQ flips stage; scoring-path ruling. §1.
- **Did not touch the 45 historical `yfinance/*` provenance rows** — ruled untouched.
- **Did not delete `probe_mu_edgar.py`** — not named in ruling 4.
- **Did not touch the census doc** (`docs/2026-08-28-closer.md` §8), including the items 1/3
  annotation left over from the previous session. The order parks census dispositions to the
  acceptance session. **Items 1, 3, 9, 20, 25, 26 and 27 are now all stale** and should be
  reconciled there.
- **Stopped after (f)** as ordered.

## 8. OPEN

1. `revenue_zero` admission — evidence above, Vic's call.
2. The general stale-stage condition; the C `YOUNG` ruling. Unchanged.
3. Census §8 needs reconciling at the acceptance session — 7 items stale.
4. The ETF guard cannot catch a fund whose payload omits `isEtf`. Bounded, named.
