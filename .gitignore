# Secrets — never commit
.env

# Runtime artifacts
caliber.db
*.db
*.db-shm
*.db-wal
*.bak
caliber.db.*

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Local Claude Code config (launch.json, scratchpad)
.claude/

# Testing / tooling
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# OS
.DS_Store
Thumbs.db

# ARCHIVED DATED SNAPSHOTS — large recorded payloads kept for REPRODUCIBILITY, never
# pushed (same standing ruling as the database artifacts above: they stay local).
# Each subdirectory carries a README naming the order that created it.
# .scratch_lly/ was retired into .snapshots/l4d1-lly-2026-08-21/ at the L-4d.1 close
# (2026-08-22); the two scripts additionally have COMMITTED copies under tools/, so the
# analysis method survives in git even if the snapshot is lost.
.snapshots/

# L-4f step-1 scope scratch (read-only measurement). Cleaned at the L-4f close.
.scratch_l4f/

# L-4f step-1 scope scratch (read-only measurement). Cleaned at the L-4f close.
.scratch_l4f/
